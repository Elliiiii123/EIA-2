namespace Einkaufsliste {

    export function generateContent(_data:Data) {

        for (let entry in _data){
            let products: Product = _data[entry];

                    
        }
    }
}